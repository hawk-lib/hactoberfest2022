//check whether a number is negative, positive
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("Enter you no.");
    scanf("%d",&a);
    if (a>0) printf("no. is possitive");
    else printf("no. is negative");
    return 0;
}
