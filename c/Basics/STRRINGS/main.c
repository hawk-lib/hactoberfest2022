#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n;
    scanf("%d",&n);
    char *arr;
    for (int i=0;i<n;i++)
        scanf("%s",arr[i]);
    for (int i=0;i<n;i++)
        printf("%s",arr[i]);

}
